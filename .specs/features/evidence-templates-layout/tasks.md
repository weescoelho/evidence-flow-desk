# Tarefas: templates layout (`evidence-templates-layout`)

**Spec:** [spec.md](./spec.md)

| Tarefa | Estado |
|--------|--------|
| T1 — Coluna `layout_key`, API create/set, variantes CSS no `buildEvidencePrintHtml` | ✅ |
| T2 — UI biblioteca + painel de gestão (select por linha) | ✅ |

**Gate:** `cargo test`, `pnpm test && pnpm build`
