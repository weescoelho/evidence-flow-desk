/**
 * @deprecated Use `ScopeCommitsStep` para o passo 2 (export directo abaixo).
 * Alias `BranchList` mantido por compatibilidade.
 */
export {
  ScopeCommitsStep,
  ScopeCommitsStep as BranchList,
} from "./scope-commits-step";
